import { SearchByCatPipe } from './search-by-cat.pipe';

describe('SearchByCatPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByCatPipe();
    expect(pipe).toBeTruthy();
  });
});
